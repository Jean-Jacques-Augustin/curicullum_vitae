import { createTheme } from "@mui/material/styles";

const Theme_light = createTheme({
     palette: {
          mode: "dark",
          primary: {
               main: "#3949ab",
          },
          secondary: {
               main: "#d32f2f",
          },
     },
});

export default Theme_light;
