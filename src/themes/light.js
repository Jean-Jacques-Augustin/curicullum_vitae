import { createTheme } from "@mui/material/styles";

const Theme_light = createTheme({
     palette: {
          mode: "light",
          primary: {
               main: "#0052cc",
          },
          secondary: {
               main: "#edf2ff",
          },
     },
});

export default Theme_light;
