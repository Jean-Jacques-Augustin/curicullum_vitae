import { createTheme } from "@mui/material/styles";

const Light_theme = createTheme({
     palette: {
          mode: "light",
          primary: {
               main: "#0052cc",
          },
          secondary: {
               main: "#edf2ff",
          },
     },
});

export default Light_theme;
